{
  "name": "ctech-lbalancer",
  "version": "0.1.0",
  "private": true,
  "description": "Lowest-cost IPv6-only HAProxy edge for CTech services",
  "scripts": {
    "build": "tsc --noEmit",
    "synth": "cdk synth",
    "test": "node --require ts-node/register --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "aws-cdk": "^2.1126.0",
    "aws-cdk-lib": "^2.258.0",
    "constructs": "^10.6.0",
    "ts-node": "^10.9.2",
    "typescript": "~6.0.3"
  }
}
